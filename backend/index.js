// Legacy entrypoint retained for compatibility. The application has been refactored to a
// modular MVC layout. The new server entrypoint is ./server.js which starts
// the Express application configured in ./src/app.js.

require('./server');
